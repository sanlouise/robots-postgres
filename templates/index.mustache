<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <link rel="stylesheet" href="/styles.css">
    <link href="https://fonts.googleapis.com/css?family=Open+Sans" rel="stylesheet">
    <title>All Robots</title>
  </head>
  <body>

    {{#users}}
    <a href="/info/{{username}}">
      <div class="user">

        <img src="{{avatar}}" />
        <h3>{{username}}</h3>

        {{#job}}
          <p class="grey">{{job}}</p>
        {{/job}}

        {{^job}}
          <p class="red">Available for hire</p>
        {{/job}}
      </div>
    </a>
    {{/users}}

  </body>
</html>
